# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/  # Python虚拟环境
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# 数据文件（保留必要的数据，只排除过大的索引文件）
# student_handbook_full.json - 保留（API依赖）

# ⭐ 索引文件：本地动态生成，不上传到Git
# 其他同学部署后可以运行 build-index.js 脚本重新生成
unified_index.json  # 统一索引文件（动态生成）
retrieval_index.json  # 备用索引文件（动态生成）
embedding_cache.json  # 向量缓存（动态生成）

# ✨ 额外排除的缓存和大文件
backend/embedding_cache.json  # embedding缓存，应该动态生成
backend/document_chunks.json  # 与根目录重复，只保留根目录的
embedding_cache.json          # 根目录缓存
neishichat_reference/         # 参考资料目录，上传时不需要
# 索引备份文件
retrieval_index.json.old*
文档库/indexes/retrieval_index.json.backup*

# Excel 数据文件（排除用户数据，保留示例数据）
# *.xlsx
# *.xls
个人信息.xlsx
学院信息.xlsx
班级信息.xlsx

# 图片文件（截图等）
*.png
*.jpg
*.jpeg
*.gif

# 敏感信息
coze个人令牌.txt
.env
*.key
secrets/

# 缓存文件
embedding_cache.json
*.cache

# 保留重要的 txt 文件：
# - requirements.txt (Python依赖)
# - 各种提示词文件（项目文档）

# 导出目录
exports/

# 临时和测试文件
test.html
kill-all.bat
stop.bat
restart.bat
startup.bat
启动内网穿透.bat
# test-questions.js ✨ 保留！这是重要的准确性测试文件
test-temp-*.js  # 只排除临时测试文件
*_backup*
*_backup.*
student_handbook_full.json_*
"~$"  # Word临时文件
相关文档/*.docx  # Word文档，大文件不上传
相关文档/~$*  # Word临时文件
databaseRagService.js  # 已废弃的数据库RAG服务
databaseSearchService.js  # 已废弃的数据库搜索服务
textCleaner.js  # 已废弃的文本清理服务
quick-index-build.js  # 临时构建脚本
efficient-rebuild.js  # 临时重建脚本
import-documents.js  # 临时导入脚本

# ngrok 工具
ngrok-v3-stable-windows-amd64/

# IDE 配置
.vscode/
.idea/
*.swp
*.swo
*~

# 操作系统文件
.DS_Store
Thumbs.db
desktop.ini

# Playwright MCP
.playwright-mcp/

# Spec Workflow（可选，根据需要调整）
.spec-workflow/

# 日志文件
*.log

# 数据库文件
*.db
*.sqlite
*.sqlite3
# database/ - 移除此行，需要将数据库脚本部署到GitHub ✨

# node_modules（如果有前端构建）
node_modules/
