# 2026-04-11 下午工作记录

## 时间线

### 14:00-18:00 发现API key失效 → 紧急修复

**问题**: ZHIPU API key `bfcc620687641d1bcfd88b0a38072f8.mzTaQmnUZq4TH2fn` 返回 401 认证失败

**发现备用key**: `neishichat_reference/backend/.env` 中有 `2ea879752b194d46ace6112688271c1b.vBLPL8iqZ70dqdEr` → 验证有效！

**修复**:
- 更新 `E:\外包\教育系统智能体\backend\.env` → `ZHIPU_API_KEY`
- 更新 `E:\外包\教育系统智能体\后台管理系统\backend\.env` → `ZHIPUAI_API_KEY`
- 重启前台后端 (port 3000) 和后台后端 (port 3005)
- Rerank API 429 余额不足，降级到本地重排序，正常工作

**验证结果**:
- ✅ 检索"重修" → 5个相关文档，0.49-0.53相似度
- ✅ 检索"奖学金" → 置信度1.0，详细回答
- ✅ 检索"考试作弊" → 置信度1.0，详细回答处分等级

**Git提交**: `e8abd1d` - fix: 紧急修复RAG检索 - 更新API key、降级minScore、统一索引路径

---

### 18:00-20:00 系统性代码分析 → 发现P0严重问题

**委托代理分析**: 读取了所有核心RAG文件，发现多个致命问题：

#### 🔴 最严重问题：RAG检索不区分文档

**现象**: 268个chunks（252学生手册+16宠物系统）全部混在一起，检索时遍历全部，无documentId过滤

**影响**: 用户问"宠物领养系统"→可能召回学生手册内容→答案混乱

**涉及文件**:
- `retrievalEngine.js` - retrieve()无documentId过滤
- `ragService.js` - answer()不传documentId
- `unifiedIndexManager.js` - retrieve()无过滤
- `unifiedRagService.js` - ask()无documentId参数
- `ragV2.js` - API不支持documentIds
- `Chat.jsx` - 无文档选择器UI

#### 🔴 硬编码绝对路径

- `documentRegistry.js:33` → `UPLOAD_DIR = 'E:/外包/教育系统智能体/相关文档'`
- `knowledgeManagement.ts:227` → 硬编码文件名
- `ragService.js:39` → `chunksPath` 硬编码旧路径

#### 🔴 API不支持指定文档查询

所有 `/api/rag/answer` 只接收 `question`，不支持 `documentIds`

---

## 用户确认的修复优先级

**P0（必须修）**:
1. RAG检索添加documentIds过滤
2. API支持documentIds参数
3. 前端添加文档选择器
4. 修复所有硬编码路径

**P1（提升健壮性）**:
1. 废弃旧indexManager，双索引合一
2. 并发写入加锁
3. 修复rerank硬编码权重
4. 添加API鉴权

---

## 关键文件速查

| 文件 | 路径 |
|------|------|
| 前台RAG服务 | `E:\外包\教育系统智能体\backend\services\ragService.js` |
| 前台检索引擎 | `E:\外包\教育系统智能体\backend\services\retrievalEngine.js` |
| 前台API路由 | `E:\外包\教育系统智能体\backend\src\routes\ragV2.js` |
| 前台聊天页面 | `E:\外包\教育系统智能体\frontend\src\pages\Chat.jsx` |
| 后台索引管理 | `E:\外包\教育系统智能体\后台管理系统\backend\src\services\unifiedIndexManager.js` |
| 后台RAG服务 | `E:\外包\教育系统智能体\后台管理系统\backend\src\services\unifiedRagService.js` |
| 后台文档路由 | `E:\外包\教育系统智能体\后台管理系统\backend\src\routes\documentRegistry.js` |

---

## 当前系统状态

**有效API key**: `2ea879752b194d46ace6112688271c1b.vBLPL8iqZ70dqdEr`
**统一索引**: `E:\外包\教育系统智能体\文档库\indexes\retrieval_index.json` (268 chunks)
**前台后端**: 运行中 (Port 3000)
**后台后端**: 运行中 (Port 3005)

---

**保存时间**: 2026-04-11 20:00
**下次继续**: 从P0问题1（RAG检索添加documentIds过滤）开始修复
